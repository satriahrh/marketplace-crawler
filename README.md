# Marketplace Crawler

## Prerequisite

1. Ruby
    ```shell script
    rvm use 2.6.3@marketplace-crawler --create
    ```
2. Bundle
    ```shell script
    gem install bundler
    ```
3. Bundle Install
    ```shell script
    bundle install
    ```